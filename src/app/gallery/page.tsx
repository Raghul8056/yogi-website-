import InfiniteCanvas from '@/components/InfiniteCanvas';

export const metadata = {
  title: 'Gallery | Yogi AI',
  description: 'Immersive infinite canvas exploring spatial interfaces.',
};

export default function GalleryPage() {
  return <InfiniteCanvas features={[]} />;
}
